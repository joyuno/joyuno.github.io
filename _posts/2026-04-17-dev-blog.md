---
layout: post
title: "2026-04-17 (금) 개발일지"
date: 2026-04-17 00:00:00 +0900
categories: [dev-blog]
tags: [dev-blog, til]
---

오늘 작업한 내용을 간단히 정리한다.

**포트폴리오** 프로젝트에서 `LG유플러스_MVNO_크롤링_프로젝트_경험기술서.md` 등을 수정했다.

**드롭쉬핑자동화** 프로젝트에서 `api.ts, product.controller.ts, page.tsx, docker-compose.yml, gcp-setup.sh` 등을 수정했다.

**드롭쉬핑자동화** 프로젝트에서 `trend.module.ts, schema.prisma, image-process.service.ts, trend-import.processor.ts, ai.module.ts` 등을 수정했다.

**드롭쉬핑자동화** 프로젝트에서 `trend.module.ts, trend-filter.service.ts, queue.module.ts, schema.prisma, trend-cron.service.ts` 등을 수정했다.

**드롭쉬핑자동화** 프로젝트에서 `notification.module.ts, order.service.ts, checkout.service.ts, notification.service.ts, order.module.ts` 등을 수정했다.

**드롭쉬핑자동화** 프로젝트에서 `page.tsx, page.tsx, page.tsx, page.tsx, page.tsx` 등을 수정했다.

**드롭쉬핑자동화** 프로젝트에서 `page.tsx, page.tsx, page.tsx, page.tsx, page.tsx` 등을 수정했다.

**드롭쉬핑자동화** 프로젝트에서 `cart-store.ts, postcss.config.mjs, page.tsx, tailwind.config.ts, tsconfig.json` 등을 수정했다.

**드롭쉬핑자동화** 프로젝트에서 `page.tsx, page.tsx, page.tsx, page.tsx, page.tsx` 등을 수정했다.

**드롭쉬핑자동화** 프로젝트에서 `product.ts, order.ts, inventory.ts` 등을 수정했다.

**사주** 프로젝트에서 `access.json` 등을 수정했다.

**사주** 프로젝트에서 `settings.local.json, .gitignore, telegram_notify.sh` 등을 수정했다.

**소명처리시스템** 프로젝트에서 `reports.py, report_generator.py, useCases.ts, pdf_generator.py, config.py` 등을 수정했다.

**소명처리시스템** 프로젝트에서 `reports.py, reports.ts, report_generator.py, pdf_generator.py, ExplanationReportTab.tsx` 등을 수정했다.

**bit-scalp** 프로젝트에서 `access.json, 8776577379` 등을 수정했다.

**bit-scalp** 프로젝트에서 `.env, MEMORY.md, __init__.py, project_overview.md, repository.py` 등을 수정했다.

**bit-scalp** 프로젝트에서 `settings.local.json` 등을 수정했다.

**bit-scalp** 프로젝트에서 `.env, __init__.py, predictor.py, app.py, telegram_bot.py` 등을 수정했다.


### 오늘의 한줄

AI 요약 생성에 실패하여 간략한 기록만 남긴다.